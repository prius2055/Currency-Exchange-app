import { NavLink } from 'react-router-dom';

const CurrencyDetail = () => {
  console.log('details');
  return <NavLink to="currency-detail">currency detail</NavLink>;
};

export default CurrencyDetail;
